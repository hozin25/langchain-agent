# Copy this file to `.env` and fill in your keys.
# The agent reads these at launch (loaded via dotenv in the main process).

# --- GLM (智谱) — required, used by default ---
# Get your key at https://open.bigmodel.cn/
GLM_API_KEY=xxxxxxxx.xxxxxxxxxxxxxxxx
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# --- DeepSeek — required for deepseek-* models ---
# Get your key at https://platform.deepseek.com/
DEEPSEEK_API_KEY=sk-xxxxxxxx
DEEPSEEK_BASE_URL=https://api.deepseek.com

# --- Deprecated providers (kept for reference; not wired into the UI) ---
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
# OPENAI_API_KEY=sk-xxxxxxxx
# DEFAULT_PROVIDER=anthropic
# DEFAULT_MODEL=claude-sonnet-4-6

# --- Tavily — required for web_search tool ---
# Get your key at https://tavily.com/
TAVILY_API_KEY=tvly-xxxxxxxx

# --- HTTP proxy — set if this machine needs a proxy to reach the internet ---
# web_fetch / web_search run in the main process on Node's fetch (undici), which
# ignores the Windows system proxy. If your network needs a proxy (e.g. a local
# clash/v2ray at 127.0.0.1:7892), uncomment and set these — otherwise every
# fetch fails with UND_ERR_CONNECT_TIMEOUT (surfaced as "Request failed: fetch failed").
# HTTPS_PROXY=http://127.0.0.1:7892
# HTTP_PROXY=http://127.0.0.1:7892
